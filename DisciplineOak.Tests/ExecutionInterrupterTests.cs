using DisciplineOak.Execution.Core;
using DisciplineOak.Execution.Task.Decorator;
using DisciplineOak.Model.Task.Decorator;
using DisciplineOak.Model.Task.Leaf.Action;
using NUnit.Framework;

namespace DisciplineOak.Tests
{
	[TestFixture]
	public class ExecutionInterrupterTests
	{
		[Test]
		public void When_interrupting_then_reaction()
		{
//			var modelInterrupter = new ModelInterrupter(null, new ModelOf<MyAction>(null));
//			var executor = BTExecutorFactory.CreateBTExecutor(modelInterrupter);
//			var executionInterrupter = new ExecutionInterrupter(modelInterrupter, executor, null);
//
//			executor.Tick();
//			executor.Tick();
//
//			executionInterrupter.Interrupt(Status.Success);
			//			Assert.AreEqual(expexted, actual);
		}

	}
}
